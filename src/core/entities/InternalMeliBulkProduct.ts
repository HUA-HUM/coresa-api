export class InternalMeliBulkProduct {
  meli_item_id: string;
  seller_id: string;
  sku: string;
  title: string;
  price: number;
  available_quantity: number;
  status: string;
  raw_payload: Record<string, unknown>;
}
